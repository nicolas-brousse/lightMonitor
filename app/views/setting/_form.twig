{% import "helpers/forms.twig" as forms %}

<form action="{{ form.action }}" method="post">
  <fieldset class="column-left">

    {{ forms.input('servername', form.servername, none, 'Servername', none, none, 'This information is just visual, it doesn\'t match the reverse of the server.', 'medium') }}

    {{ forms.input('ip', form.ip, none, 'IP address', none, {'text': 'Invalid format (xxx.xxx.xxx.xxx)', 'type': 'error'}) }}

    {{ forms.select('protocol', form.protocol, form.protocols, 'Protocol to get informations', none, none, none, none) }}

    {{ forms.input('port', form.port, none, 'Port', none, none, none) }}

    {{ forms.input('login', form.login, none, 'User', none, none, none) }}

    {{ forms.input('pass', form.pass, 'password', 'Password', none, none, none) }}

    {{ forms.submit() }}

  </fieldset>
  <fieldset class="column-right">
    <p>
      <label>Software to monitor</label>
      {{ forms.checkbox('ftp', form.ftp, 'FTP', none, '(port 21)') }}
      {{ forms.checkbox('ssh', form.ssh, 'SSH', none, '(port 22)') }}
      {{ forms.checkbox('dns', form.dns, 'DNS', none, '(port 53)') }}
      {{ forms.checkbox('http', form.http, 'HTTP', none, '(port 80)') }}
      {{ forms.checkbox('smtp', form.smtp, 'SMTP', none, '(port 25)') }}
      {{ forms.checkbox('pop', form.pop, 'POP', none, '(port 110)') }}
      {{ forms.checkbox('imap', form.imap, 'IMAP', none, '(port 143)') }}
    </p>
  </fieldset>

  <div class="clear"></div><!-- End .clear -->
</form>